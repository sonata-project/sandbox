# Enable Doctrine to map the provided entities
sonata_order:
    class:
        order:         AppBundle\Entity\Commerce\Order
        order_element: AppBundle\Entity\Commerce\OrderElement
        customer:      AppBundle\Entity\Commerce\Customer

doctrine:
    orm:
        entity_managers:
            default:
                mappings:
                    SonataOrderBundle: ~
