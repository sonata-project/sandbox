# Enable Doctrine to map the provided entities
sonata_invoice:
    class:
        invoice:         AppBundle\Entity\Commerce\Invoice
        invoice_element: AppBundle\Entity\Commerce\InvoiceElement
        customer:        AppBundle\Entity\Commerce\Customer
        order_element:   AppBundle\Entity\Commerce\OrderElement

doctrine:
    orm:
        entity_managers:
            default:
                mappings:
                    SonataInvoiceBundle: ~
